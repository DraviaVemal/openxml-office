include "../global_2007/consolidated.fbs";

namespace openxml_office_fbs.spreadsheet_2007;

// Define the ExcelProperties table with three fields
table ExcelProperties {
  settings: ExcelSettings;
  theme: global_2007.ThemePalletModel;
  core_properties: global_2007.CorePropertiesModel;
}

// Define the ExcelSettings table
table ExcelSettings {
  // Add fields here as needed
}

// Root type
root_type ExcelProperties;
